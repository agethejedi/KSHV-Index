import type { KSResult } from '../types';
import { formatMoney, formatNumber } from '../lib/format';
export function MetricCards({ result }: { result: KSResult | null }) { return <div className="metrics-grid"><div className="card metric-card"><h4>KS Demand Pool</h4><div className="metric-value">{result ? formatNumber(result.demandPool) : '—'}</div><p>Estimated statistically plausible partner pool in the selected metro.</p></div><div className="card metric-card"><h4>KS Competition</h4><div className="metric-value">{result ? formatNumber(result.competitionPool) : '—'}</div><p>Estimated number of comparable same-sex profiles competing in the same market.</p></div><div className="card metric-card"><h4>Local context</h4><div className="metric-value small">{result ? formatMoney(result.summary.metroMedianIncome) : '—'}</div><p>Metro median household income from ACS, used as the income anchor in the score model.</p></div></div>; }
