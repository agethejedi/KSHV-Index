import type { KSResult } from '../types';
import { formatMoney } from '../lib/format';
export function ScorePanel({ result }: { result: KSResult | null }) { return <div className="card score-card"><div className="score-value">{result ? result.ksScore : '—'}</div><div className="score-label">KS Score</div><div className="score-meta">{result ? `Top ${Math.max(1,100-result.ksScore)}% in ${result.metroLabel}` : 'Choose a metro and calculate'}</div>{result && <div className="score-mini-stats"><div><span>Percentile</span><strong>{Math.round(result.percentile*100)}th</strong></div><div><span>Median HH income</span><strong>{formatMoney(result.summary.metroMedianIncome)}</strong></div><div><span>Target age range</span><strong>{result.summary.ageTargetMin}–{result.summary.ageTargetMax}</strong></div></div>}</div>; }
